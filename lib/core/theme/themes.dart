import 'package:flutter/material.dart';

ThemeData themes() {
  return ThemeData(
      colorScheme: ColorScheme.light(),
      scaffoldBackgroundColor: Color(0xFFFFFFFFF),
      primaryColor: Color(0xFFFECD00));
}
